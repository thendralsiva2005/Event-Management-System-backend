spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/event_db
    username: postgres
    password: 2005
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080